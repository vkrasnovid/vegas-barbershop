<section id="hero" class="relative min-h-screen flex items-center justify-center overflow-hidden bg-brand-black">
  <!-- Background Image with overlay -->
  <!-- Background Image with overlay -->
  <div class="absolute inset-0 hero-parallax" style="background-image: url('/images/hero.webp'); background-size: cover; background-position: center;">
    <div class="absolute inset-0 bg-gradient-to-br from-brand-black/90 via-brand-black/70 to-brand-black/50"></div>
  </div>

  <!-- Content -->
  <div class="relative z-10 text-center px-4 max-w-4xl mx-auto">
    <!-- Badge -->
    <div class="inline-flex items-center px-4 py-1.5 rounded-full bg-brand-gold/15 border border-brand-gold/30 text-brand-gold text-[0.75rem] font-semibold tracking-[0.08em] mb-6 animate-fade-in" style="animation-delay: 0.1s;">
      Барбершоп в Энгельсе
    </div>

    <!-- Headline -->
    <h1 class="font-heading text-[clamp(2.5rem,5vw,4rem)] font-black leading-[1.1] tracking-[-0.02em] mb-4 gold-shimmer animate-fade-in" style="animation-delay: 0.2s;">
      VEGAS
    </h1>

    <!-- Subtitle -->
    <p class="font-body text-[clamp(1rem,1.2vw,1.25rem)] font-light text-brand-white/90 tracking-[0.05em] mb-3 animate-fade-in" style="animation-delay: 0.3s;">
      Искусство мужского стиля
    </p>

    <!-- Description -->
    <p class="font-body text-[clamp(0.875rem,1vw,1rem)] text-brand-gray max-w-[480px] mx-auto mb-8 animate-fade-in" style="animation-delay: 0.4s;">
      Стрижки, бритьё и уход за бородой. Только для мужчин, только с характером.
    </p>

    <!-- CTAs -->
    <div class="flex flex-col sm:flex-row items-center justify-center gap-4 animate-fade-in" style="animation-delay: 0.5s;">
      <a href="/vegas-barbershop/#booking" class="inline-flex items-center gap-2 bg-brand-gold text-brand-black text-[1rem] font-semibold px-10 py-4 rounded-full hover:bg-brand-gold-light hover:scale-[1.02] active:bg-brand-gold-dark transition-all duration-200 shadow-glow hover:shadow-glow-lg no-underline min-h-[48px]">
        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 6h18"/><path d="M8 6V4"/><path d="M16 6V4"/><rect width="18" height="16" x="3" y="8" rx="2"/><path d="M3 13h18"/></svg>
        Записаться
      </a>
      <a href="/vegas-barbershop/#services" class="inline-flex items-center gap-2 px-8 py-3.5 rounded-full border border-brand-gold text-brand-gold bg-transparent hover:bg-brand-gold hover:text-brand-black transition-all duration-300 text-[0.875rem] font-medium min-h-[48px] no-underline">
        Услуги и цены
      </a>
    </div>
  </div>

  <!-- Scroll indicator -->
  <a href="/vegas-barbershop/#services" class="absolute bottom-8 left-1/2 -translate-x-1/2 text-brand-gray animate-bounce" aria-label="Прокрутить вниз">
    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m6 9 6 6 6-6"/></svg>
  </a>

  <!-- Gold gradient divider -->
  <div class="absolute bottom-0 left-0 right-0 h-[1.5px] bg-gradient-to-r from-transparent via-brand-gold/50 to-transparent"></div>
</section>

<style is:inline>
  @keyframes fadeIn {
    from { opacity: 0; transform: translateY(10px); }
    to { opacity: 1; transform: translateY(0); }
  }
  .animate-fade-in {
    opacity: 0;
    animation: fadeIn 0.6s ease-out forwards;
  }
  .animate-bounce {
    animation: bounce 2s infinite;
  }
  @keyframes bounce {
    0%, 100% { transform: translateX(-50%) translateY(0); }
    50% { transform: translateX(-50%) translateY(8px); }
  }
</style>
